streamlit>=1.33
python-dotenv>=1.0

langchain>=0.2.15
langchain-core>=0.2.38
langchain-openai>=0.1.22
langchain-community>=0.2.12
langchain-text-splitters>=0.2.2
openai>=1.30.0

# Avoid: "Descriptors cannot be created directly" (old _pb2 vs protobuf 4+)
protobuf>=3.20.0,<4.0.0

chromadb>=0.5.5
langchain-chroma>=0.1.0
sentence-transformers>=3.0.1
torch
# transformers 惰性导入可能加载视觉子模块，Streamlit watcher 会触发 → 需 torchvision
torchvision
# 可选轻量向量（无需 torch）：fastembed>=0.4.0，并设 RAG_EMBEDDING_PROVIDER=fastembed
